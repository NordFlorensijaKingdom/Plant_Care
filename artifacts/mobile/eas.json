{
  "cli": {
    "version": ">= 16.0.0",
    "appVersionSource": "local"
  },
  "build": {
    "preview": {
      "installCommand": "corepack enable && corepack prepare pnpm@10.28.1 --activate && pnpm install --frozen-lockfile",
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "installCommand": "corepack enable && corepack prepare pnpm@10.28.1 --activate && pnpm install --frozen-lockfile",
      "android": {
        "buildType": "app-bundle"
      }
    }
  }
}
